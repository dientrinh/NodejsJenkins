{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=html --reporter=cobertura --reporter=text mocha",
    "coverage": "nyc report --reporter=cobertura | coveralls",
	"test-jenkins": "mocha --colors --reporter mocha-jenkins-reporter"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.0.2",
    "mocha": "^5.2.0",
    "mocha-jenkins-reporter": "^0.4.1",
    "mocha-junit-reporter": "^1.18.0",
    "nyc": "^13.1.0",
    "should": "^13.2.3",
    "supertest": "^3.3.0"
  }
}
